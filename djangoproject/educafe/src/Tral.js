import React from 'react'

const Tral = ({formData}) => {
  const dataObject = {
    name: formData.name,
    email: formData.email,
    userName: formData.username,
  };

  // You can now work with the dataObject as needed

  return null; // Return null to indicate that this component doesn't render anything
}

export default Tral